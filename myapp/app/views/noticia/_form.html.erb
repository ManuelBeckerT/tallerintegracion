<%= form_for(noticium) do |f| %>
  <% if noticium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(noticium.errors.count, "error") %> prohibited this noticium from being saved:</h2>
      <ul>
      <% noticium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo, :size => '100'%>

  </div>

  <div class="form-group">

    <%= f.label :bajada %>
    <%= f.text_field :bajada, :maxlength=>"200", :size => "100"%>
  </div>

  <div class="field">
    <%= f.label :contenido %>
    <%= f.text_area :contenido %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
